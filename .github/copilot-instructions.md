# Repository instructions

Read `AGENTS.md` before editing this repository. Preserve the existing public APIs of UI primitives unless the task explicitly changes them. For component changes, update the matching documentation and registry payload when applicable, then run `npm run check`.

Use semantic HTML and current WAI-ARIA Authoring Practices for interactive widgets. Do not modify generated files, lockfiles, or unrelated components.
